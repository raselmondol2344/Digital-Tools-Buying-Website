import { use } from "react";


const Models = ({getPromise}) => {
    const model = use (getPromise)
    console.log(model);
    
    return (
        <div>
         
            
        </div>
    );
};

export default Models;